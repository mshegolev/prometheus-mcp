---
phase: 10-dependency-mapping-health
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified: [src/prometheus_mcp/dependency.py, src/prometheus_mcp/models.py]
autonomous: true
requirements: [DEP-01, DEP-02, DEP-03]
user_setup: []

must_haves:
  truths:
    - AI agents can visualize interoperation between services in different regions/clusters
    - AI agents can assess dependency resilience through synthetic health probing
    - AI agents can differentiate between normal and failure-state interactions in real-time
    - Dependency fragility assessments inform load shedding recommendations
  artifacts:
    - path: "src/prometheus_mcp/dependency.py"
    provides: "Cross-cluster visualization and health probing capabilities"
    min_lines: 400
    - path: "src/prometheus_mcp/models.py"
    provides: "Enhanced data models for health probing results"
    contains: "class HealthProbeResult"
    - path: "src/prometheus_mcp/models.py"
    provides: "Enhanced data models for health probing results"
    contains: "class SyntheticProbe"
  key_links:
    - from: "src/prometheus_mcp/dependency.py"
    to: "src/prometheus_mcp/dependency.py"
    via: "dependency graph extension"
    pattern: "DependencyGraphBuilder"
    - from: "src/prometheus_mcp/dependency.py"
    to: "src/prometheus_mcp/rca.py"
    via: "health assessment integration"
    pattern: "AnomalyDetector"

verification:
  - Cross-cluster dependency visualization correctly represents service relationships across instances
  - Synthetic health probing produces reliable resilience assessments
  - Real-time dependency maps accurately differentiate normal from failure states
  - Load shedding recommendations are based on quantified fragility assessments

success_criteria:
  - Cross-cluster dependency visualization shows interoperation between services in different regions
  - Synthetic health probing system assesses dependency resilience under various conditions
  - Real-time dependency maps differentiate between normal and failure-state interactions
  - Load shedding recommendations are based on dependency fragility assessments
---
<objective>
Implement cross-cluster dependency visualization and synthetic health probing system.

Purpose: Enable AI agents to visualize service dependencies across clusters and proactively assess their health through synthetic probing.
Output: Enhanced dependency mapping with cross-cluster visualization and health assessment capabilities
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-dependency-mapping-health/CONTEXT.md
@.planning/phases/09-root-cause-analysis-tools/CONTEXT.md

From src/prometheus_mcp/models.py (after completion of Plan 01):
```python
# After completion of Plan 01, these will exist:
class ServiceNode(TypedDict):
    # ... service node fields
    
class DependencyEdge(TypedDict):
    # ... dependency edge fields
    
class DependencyGraph(TypedDict):
    # ... dependency graph fields
```

From src/prometheus_mcp/dependency.py (after Plan 01):
```python
class TrafficCorrelator:
    def __init__(self, registry: InstanceRegistry) -> None:
        # ...
        
    def analyze_traffic_correlations(
        self,
        start_time: str,
        end_time: str,
        service_filter: str | None = None
    ) -> CorrelationAnalysisResult:
        # ...

class DependencyGraphBuilder:
    def __init__(self, registry: InstanceRegistry) -> None:
        # ...
        
    def build_dependency_graph(
        self,
        correlation_data: CorrelationAnalysisResult
    ) -> DependencyGraph:
        # ...
```

From src/prometheus_mcp/rca.py:
```python
class AnomalyDetector:
    def __init__(self, registry: InstanceRegistry) -> None:
        # ...
        
    def detect_metric_anomalies(
        self, 
        metric_names: list[str], 
        start_time: str, 
        end_time: str,
        sensitivity: float = 0.5
    ) -> AnomalyDetectionResult:
        # ...
```
</context>

<tasks>

<task type="auto">
  <name>task 1: implement cross-cluster dependency visualization</name>
  <files>src/prometheus_mcp/dependency.py, src/prometheus_mcp/models.py</files>
  <action>
Implement cross-cluster dependency visualization capabilities:
1. Extend models.py with `ClusterInfo` TypedDict including:
   - Cluster identification and metadata fields
   - Geographic and logical grouping information
   - Instance membership and health status
2. Enhance `DependencyGraph` model in models.py to include:
   - Cluster-aware service grouping
   - Cross-cluster dependency relationships
   - Geographic visualization metadata
3. Add `CrossClusterVisualizer` class in dependency.py that:
   - Takes an InstanceRegistry in its constructor
   - Implements `visualize_cross_cluster_dependencies` method that:
     - Accepts a dependency graph and visualization parameters
     - Groups services by cluster with appropriate coloring
     - Identifies and highlights cross-cluster dependencies
     - Generates cluster-aware layout coordinates
     - Returns visualization-ready graph data
4. Helper methods for:
   - Cluster affinity mapping for multi-region deployments
   - Logical grouping based on namespace, team, or business domain
   - Instance-level attribution for debugging distributed issues
   - Visualization coordinate generation for graph layouts
5. Integration with existing federation patterns for cross-instance data

Follow the same architectural patterns as other dependency components.
  </action>
  <verify>
    <automated>python -c "from prometheus_mcp.dependency import CrossClusterVisualizer; hasattr(prometheus_mcp.models, 'ClusterInfo') and print('CrossClusterVisualizer class and ClusterInfo model exist')"</automated>
  </verify>
  <done>Cross-cluster dependency visualization shows interoperation between services in different regions</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: implement synthetic health probing system</name>
  <files>src/prometheus_mcp/dependency.py, src/prometheus_mcp/models.py</files>
  <behavior>
    - Test 1: Generates lightweight synthetic requests with minimal system impact
    - Test 2: Monitors response times and error rates for dependency health assessment
    - Test 3: Produces resilience scores based on response patterns under stress
    - Test 4: Handles probe failures gracefully without affecting production traffic
  </behavior>
  <action>
Implement synthetic health probing system in dependency.py:
1. Extend models.py with `SyntheticProbe` and `HealthProbeResult` TypedDict classes:
   - `SyntheticProbe`: Probe configuration including target, frequency, and parameters
   - `HealthProbeResult`: Probe results including response metrics and health scores
2. Add `HealthProber` class that takes an InstanceRegistry:
   - Implement `configure_synthetic_probe` method for probe setup
   - Implement `execute_health_probe` method that:
     - Generates lightweight synthetic requests to test dependency paths
     - Monitors response times, error rates, and other health metrics
     - Calculates resilience scores based on response patterns
     - Returns HealthProbeResult with detailed findings
3. Helper methods for:
   - Synthetic request generation with configurable payloads
   - Response time and error rate monitoring
   - Statistical analysis for health assessment validity
   - Resource impact minimization for production environments
4. Integration with existing federation infrastructure for cross-instance probing
5. Proper error handling for network issues and probe failures

Ensure implementation follows established patterns for reliability and performance.
  </action>
  <verify>
    <automated>python -c "from prometheus_mcp.dependency import HealthProber; hasattr(prometheus_mcp.models, 'SyntheticProbe') and hasattr(prometheus_mcp.models, 'HealthProbeResult') and print('HealthProber class and probe models exist')"</automated>
  </verify>
  <done>Synthetic health probing system assesses dependency resilience under various conditions</done>
</task>

<task type="auto">
  <name>task 3: implement real-time dependency state differentiation</name>
  <files>src/prometheus_mcp/dependency.py</files>
  <action>
Implement real-time dependency state differentiation capabilities:
1. Add `StateDifferentiator` class in dependency.py that takes an InstanceRegistry:
2. Implement `differentiate_dependency_states` method that:
   - Accepts current dependency graph and alert data
   - Analyzes live metric streams for state transition detection
   - Identifies normal vs. failure-state interactions using pattern recognition
   - Highlights affected service relationships in the dependency graph
   - Returns updated graph with state differentiation metadata
3. Helper methods for:
   - Live dependency graph updates reflecting current system state
   - Failure-state highlighting for affected service relationships
   - Temporal correlation analysis for identifying failure propagation
   - Anomaly detection in dependency interaction patterns
4. Integration with existing RCA anomaly detection for enhanced accuracy
5. Real-time update mechanisms with efficient delta computation
6. Proper handling of concurrent state changes and race conditions

Maintain consistency with existing real-time processing patterns in the codebase.
  </action>
  <verify>
    <automated>python -c "from prometheus_mcp.dependency import StateDifferentiator; print('StateDifferentiator class exists')"</automated>
  </verify>
  <done>Real-time dependency maps differentiate between normal and failure-state interactions</done>
</task>

</tasks>
---
phase: 10-dependency-mapping-health
plan: 03
type: execute
wave: 3
depends_on: [10-01, 10-02]
files_modified: [src/prometheus_mcp/dependency.py, src/prometheus_mcp/models.py, tests/test_dependency.py]
autonomous: true
requirements: [DEP-01, DEP-02, DEP-03]
user_setup: []

must_haves:
  truths:
    - AI agents can generate load shedding recommendations based on dependency fragility assessments
    - All dependency mapping components have comprehensive unit test coverage
    - Integration tests validate cross-cluster dependency workflows
    - Performance tests ensure dependency analysis scales with service volume
  artifacts:
    - path: "src/prometheus_mcp/dependency.py"
    provides: "Complete dependency mapping engine with load shedding recommendations"
    min_lines: 400
    - path: "tests/test_dependency.py"
    provides: "Unit tests for all dependency mapping components"
    min_lines: 300
    - path: "src/prometheus_mcp/models.py"
    provides: "Enhanced data models for load shedding recommendations"
    contains: "class LoadSheddingRecommendation"
  key_links:
    - from: "src/prometheus_mcp/dependency.py"
    to: "src/prometheus_mcp/dependency.py"
    via: "load shedding integration"
    pattern: "DependencyGraphBuilder"
    - from: "tests/test_dependency.py"
    to: "src/prometheus_mcp/dependency.py"
    via: "component testing"
    pattern: "TrafficCorrelator|DependencyGraphBuilder|HealthProber"

verification:
  - Load shedding recommendations are based on quantified dependency fragility assessments
  - Unit tests cover 90%+ of dependency mapping code paths
  - Integration tests validate cross-cluster dependency workflows
  - Performance tests demonstrate sub-second response times for typical analyses

success_criteria:
  - Load shedding recommendations are generated based on dependency fragility assessments
  - Comprehensive unit test coverage for all dependency mapping algorithms
  - Integration tests validating cross-cluster dependency workflows
  - Performance benchmarks demonstrating scalable operation
---
<objective>
Implement load shedding recommendations based on dependency fragility and create comprehensive tests.

Purpose: Enable AI agents to recommend load shedding based on dependency fragility assessments and ensure implementation correctness through comprehensive testing.
Output: Complete dependency mapping engine with load shedding capabilities and test suite
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-dependency-mapping-health/CONTEXT.md

From src/prometheus_mcp/models.py (after completion of Plans 01-02):
```python
# After completion of Plans 01-02, these will exist:
class ServiceNode(TypedDict):
    # ... service node fields
    
class DependencyEdge(TypedDict):
    # ... dependency edge fields
    
class DependencyGraph(TypedDict):
    # ... dependency graph fields
    
class HealthProbeResult(TypedDict):
    # ... health probe result fields
```

From src/prometheus_mcp/dependency.py (after Plans 01-02):
```python
class TrafficCorrelator:
    def analyze_traffic_correlations(self, /* ... */) -> CorrelationAnalysisResult:
        # ...

class DependencyGraphBuilder:
    def build_dependency_graph(self, /* ... */) -> DependencyGraph:
        # ...

class CrossClusterVisualizer:
    def visualize_cross_cluster_dependencies(self, /* ... */) -> VisualizationResult:
        # ...

class HealthProber:
    def execute_health_probe(self, /* ... */) -> HealthProbeResult:
        # ...

class StateDifferentiator:
    def differentiate_dependency_states(self, /* ... */) -> DependencyGraph:
        # ...
```

From tests/test_rca.py:
```python
@responses.activate
def test_anomaly_detection_normal_data() -> None:
    # Test pattern following existing conventions
```
</context>

<tasks>

<task type="auto">
  <name>task 1: implement load shedding recommendation engine</name>
  <files>src/prometheus_mcp/dependency.py, src/prometheus_mcp/models.py</files>
  <action>
Implement load shedding recommendation engine in dependency.py:
1. Extend models.py with `LoadSheddingRecommendation` TypedDict including:
   - Recommended services for load reduction
   - Priority ranking with confidence scores
   - Estimated impact of load shedding actions
   - Alternative routing suggestions for maintained availability
2. Add `LoadSheddingAdvisor` class that takes an InstanceRegistry:
3. Implement `generate_load_shedding_recommendations` method that:
   - Accepts dependency graph, health probe results, and system metrics
   - Calculates fragility scores for individual dependency relationships
   - Performs cascading failure risk analysis for service groups
   - Generates load redistribution suggestions to protect critical dependencies
   - Produces graceful degradation pathways for maintaining system availability
   - Returns ranked LoadSheddingRecommendation list
4. Helper methods for:
   - Fragility scoring for individual dependency relationships
   - Cascading failure simulation for impact prediction
   - Load distribution optimization for maximum system resilience
   - Trade-off analysis between availability and performance objectives
5. Integration with existing health probing and dependency analysis components
6. Proper error handling for incomplete data or analysis failures

Follow established patterns for extensibility and configurability.
  </action>
  <verify>
    <automated>python -c "from prometheus_mcp.dependency import LoadSheddingAdvisor; hasattr(prometheus_mcp.models, 'LoadSheddingRecommendation') and print('LoadSheddingAdvisor class and recommendation model exist')"</automated>
  </verify>
  <done>Load shedding recommendations are generated based on dependency fragility assessments</done>
</task>

<task type="auto">
  <name>task 2: create unit tests for dependency mapping components</name>
  <files>tests/test_dependency.py</files>
  <action>
Create comprehensive unit tests for dependency mapping components:
1. Create test_dependency.py file following existing test patterns in test_rca.py
2. Test `TrafficCorrelator.analyze_traffic_correlations` with:
   - Normal traffic patterns without dependencies (should identify minimal relationships)
   - Clear service dependency patterns (should detect strong correlations)
   - Cross-cluster traffic data (should resolve service identities correctly)
   - Missing or incomplete metric data (should handle gracefully)
   - Large-scale traffic datasets (should perform efficiently)
3. Test `DependencyGraphBuilder.build_dependency_graph` with:
   - Simple linear dependency chains (should construct correctly)
   - Complex branching dependency graphs (should represent all relationships)
   - Circular dependencies (should handle without infinite loops)
   - Cross-cluster service relationships (should maintain instance attribution)
4. Test `HealthProber.execute_health_probe` with:
   - Healthy dependency paths (should return high resilience scores)
   - Degraded dependency paths (should detect performance issues)
   - Failing dependency paths (should identify error conditions)
   - Network timeout scenarios (should handle gracefully)
5. Test `LoadSheddingAdvisor.generate_load_shedding_recommendations` with:
   - Fragile dependency graphs (should recommend appropriate load reductions)
   - Stable dependency graphs (should recommend minimal or no shedding)
   - Mixed health scenarios (should prioritize critical dependencies)
   - Cascading failure risks (should suggest preventive measures)
6. Mock Prometheus client responses using responses library
7. Test edge cases:
   - Empty correlation results
   - Services with no dependencies
   - Extremely large dependency graphs
   - Services with many upstream dependencies

Follow the same testing patterns as existing modules with proper setup/teardown.
  </action>
  <verify>
    <automated>bash -c "cd /opt/develop/aiqa/mcps/prometheus-mcp && python -m pytest tests/test_dependency.py -v --tb=short"</automated>
  </verify>
  <done>Unit tests cover dependency mapping functionality with various data scenarios</done>
</task>

<task type="auto">
  <name>task 3: create integration tests for cross-cluster workflows</name>
  <files>tests/test_dependency.py</files>
  <action>
Create integration tests for cross-cluster dependency mapping workflows:
1. Test cross-cluster dependency discovery with:
   - Multi-instance Prometheus setup with related services
   - Service identity resolution across different cluster contexts
   - Geographic affinity mapping for multi-region deployments
   - Instance-level attribution for debugging distributed issues
2. Test real-time dependency state differentiation with:
   - Live metric stream simulation for state transition detection
   - Failure-state highlighting for affected service relationships
   - Temporal correlation analysis for identifying failure propagation
   - Anomaly detection integration for enhanced accuracy
3. Test load shedding recommendation workflows with:
   - Fragility assessment based on synthetic health probing
   - Cascading failure risk analysis for service groups
   - Load redistribution suggestions for system resilience
   - Trade-off analysis between availability and performance
4. Use responses library to mock:
   - Prometheus metric queries across multiple instances
   - Alertmanager alert retrieval for state differentiation
   - Synthetic probe execution for health assessment
5. Test performance characteristics:
   - Response time for typical dependency analyses
   - Memory usage with large dependency graphs
   - Concurrent access patterns for real-time updates
6. Validate output formats:
   - Structured content matches expected models
   - Cross-cluster visualization metadata is correctly formatted
   - Load shedding recommendations include actionable guidance
   - Error conditions produce appropriate messages

Ensure tests maintain the same quality standards as existing integration tests.
  </action>
  <verify>
    <automated>bash -c "cd /opt/develop/aiqa/mcps/prometheus-mcp && python -m pytest tests/test_dependency.py -v -k 'integration' --tb=short"</automated>
  </verify>
  <done>Integration tests validate cross-cluster dependency workflows with proper error handling</done>
</task>

<task type="auto">
  <name>task 4: implement dependency mapping engine facade</name>
  <files>src/prometheus_mcp/dependency.py</files>
  <action>
Implement a unified dependency mapping engine facade:
1. Add `DependencyMappingEngine` class that takes an InstanceRegistry:
2. Implement methods that combine multiple components:
   - `discover_and_map_dependencies` combining TrafficCorrelator and DependencyGraphBuilder
   - `assess_dependency_health` combining HealthProber and StateDifferentiator
   - `recommend_load_shedding` using LoadSheddingAdvisor with current system state
   - `get_cross_cluster_view` using CrossClusterVisualizer for presentation
3. Add convenience methods for common workflows:
   - End-to-end dependency analysis from raw metrics to recommendations
   - Incremental updates for real-time dependency graph maintenance
   - Bulk health assessments for system-wide dependency evaluation
4. Implement proper error handling and result aggregation:
   - Graceful degradation when individual components fail
   - Result consistency across component boundaries
   - Performance optimization through result caching
5. Add configuration options for tuning:
   - Correlation sensitivity thresholds
   - Health probe parameters
   - Load shedding aggressiveness
   - Cross-cluster visualization preferences
6. Document the facade with comprehensive examples:
   - Typical usage patterns for AI agents
   - Configuration customization examples
   - Error handling best practices
   - Performance optimization guidelines

Ensure the facade provides a clean, intuitive interface while maintaining full functionality.
  </action>
  <verify>
    <automated>python -c "from prometheus_mcp.dependency import DependencyMappingEngine; print('DependencyMappingEngine class exists')"</automated>
  </verify>
  <done>Unified dependency mapping engine provides convenient access to all capabilities</done>
</task>

</tasks>
---
phase: 10-dependency-mapping-health
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/prometheus_mcp/dependency.py, src/prometheus_mcp/models.py]
autonomous: true
requirements: [DEP-01, DEP-02, DEP-03]
user_setup: []

must_haves:
  truths:
    - AI agents can discover service relationships through traffic correlation analysis
    - AI agents can build cross-cluster dependency graphs showing service interconnections
    - AI agents can identify service dependencies with confidence scores based on correlation strength
    - Dependency graphs are updated in real-time as traffic patterns change
  artifacts:
    - path: "src/prometheus_mcp/dependency.py"
    provides: "Dynamic dependency mapper with traffic correlation analysis"
    min_lines: 400
    - path: "src/prometheus_mcp/models.py"
    provides: "Data models for dependency mapping results"
    contains: "class DependencyGraph"
    - path: "src/prometheus_mcp/models.py"
    provides: "Data models for dependency mapping results"
    contains: "class ServiceNode"
    - path: "src/prometheus_mcp/models.py"
    provides: "Data models for dependency mapping results"
    contains: "class DependencyEdge"
  key_links:
    - from: "src/prometheus_mcp/dependency.py"
    to: "src/prometheus_mcp/registry.py"
    via: "instance registry access"
    pattern: "registry\\."
    - from: "src/prometheus_mcp/dependency.py"
    to: "src/prometheus_mcp/client.py"
    via: "metric querying"
    pattern: "prometheus_client\\."
    - from: "src/prometheus_mcp/dependency.py"
    to: "src/prometheus_mcp/federation.py"
    via: "cross-instance analysis"
    pattern: "fan_out_prometheus"

threat_model:
  ## Trust Boundaries

  | Boundary | Description |
  |----------|-------------|
  | Agent→MCP Server | Untrusted input from AI agents |
  | MCP Server→Prometheus Instances | Trusted outbound requests to monitored systems |

  ## STRIDE Threat Register

  | Threat ID | Category | Component | Disposition | Mitigation Plan |
  |-----------|----------|-----------|-------------|-----------------|
  | T-10-01 | Spoofing | Metric data sources | mitigate | Validate instance authenticity through registry |
  | T-10-02 | Tampering | Dependency configuration | mitigate | Immutable configuration from trusted sources |
  | T-10-03 | Repudiation | Dependency mapping decisions | mitigate | Detailed audit logging of all mapping operations |
  | T-10-04 | Information Disclosure | System topology | mitigate | Role-based access control on instance data |
  | T-10-05 | Denial of Service | Resource-intensive analysis | mitigate | Configurable timeouts and resource limits |
  | T-10-06 | Elevation of Privilege | Tool execution | mitigate | ReadOnlyHint enforcement in MCP annotations |

verification:
  - All new classes and functions have comprehensive docstrings
  - Dependency mapper integrates with existing InstanceRegistry pattern
  - Traffic correlation handles missing/invalid metric data gracefully
  - Cross-cluster dependency resolution correctly identifies service relationships
  - Real-time updates maintain graph consistency under concurrent access

success_criteria:
  - Dynamic dependency mapper discovers service relationships through traffic correlation analysis
  - Cross-cluster dependency visualization shows interoperation between services in different regions
  - Service dependencies are identified with confidence scores based on correlation strength
  - Dependency graphs are updated in real-time as traffic patterns change
---
<objective>
Implement dynamic dependency mapper that discovers service relationships through traffic correlation analysis.

Purpose: Enable AI agents to automatically discover and map service dependencies across clusters using traffic correlation analysis rather than static configuration.
Output: New dependency mapping module with integration into existing federation infrastructure
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-root-cause-analysis-tools/CONTEXT.md
@.planning/phases/04-federation-fanout-merge/CONTEXT.md
@.planning/phases/02-instance-registry/CONTEXT.md

From src/prometheus_mcp/models.py:
```python
class QueryRangeOutput(TypedDict):
    query: str
    start: str
    end: str
    step: str
    result_type: str
    series_count: int
    total_points: int
    truncated: bool
    data: list[RangeSeries]

class RangeSeries(TypedDict):
    labels: dict[str, str]
    point_count: int
    values: list[list[float | str]]
```

From src/prometheus_mcp/federation.py:
```python
def fan_out_prometheus(
    query_func: Callable[[ClientType], T],
    clients: list[ClientType],
    *,
    instance_names: list[str] | None = None,
    timeout: float | None = None,
    max_workers: int | None = None,
) -> dict:
    """Execute a query function across multiple Prometheus/Alertmanager instances concurrently.
```

From src/prometheus_mcp/rca.py:
```python
class RCAEngine:
    def __init__(self, registry: InstanceRegistry) -> None:
        self.registry = registry
        self.anomaly_detector = AnomalyDetector(registry)
        self.dependency_traverser = DependencyTraverser(registry)
        self.change_point_detector = ChangePointDetector(registry)
        self.root_cause_ranker = RootCauseRanker(registry)
```
</context>

<tasks>

<task type="auto">
  <name>task 1: extend models with dependency mapping types</name>
  <files>src/prometheus_mcp/models.py</files>
  <action>
Extend the models.py file with new TypedDict classes for dependency mapping results:
1. Create `ServiceNode` with fields for service identification, metadata, and health status
2. Create `DependencyEdge` with fields for source/target services, strength metrics, and relationship type
3. Create `DependencyGraph` with fields for nodes, edges, and metadata including cross-cluster information
4. Create `DependencyAnalysisResult` with fields for discovered dependencies, confidence scores, and timestamps
5. Create `CrossClusterInfo` for representing cluster-specific service information
6. Ensure all new models follow the existing pattern with proper type hints and documentation
7. Add utility methods for graph manipulation and serialization

Structure these models to be compatible with existing correlation result formats and follow the same design patterns.
  </action>
  <verify>
    <automated>python -c "import prometheus_mcp.models; hasattr(prometheus_mcp.models, 'ServiceNode') and hasattr(prometheus_mcp.models, 'DependencyEdge') and hasattr(prometheus_mcp.models, 'DependencyGraph')"</automated>
  </verify>
  <done>All dependency mapping result models are defined with proper type hints and documentation</done>
</task>

<task type="auto">
  <name>task 2: implement traffic correlation analyzer</name>
  <files>src/prometheus_mcp/dependency.py</files>
  <action>
Create a new dependency.py module implementing the traffic correlation analyzer with these components:
1. `TrafficCorrelator` class that takes an InstanceRegistry in its constructor
2. `analyze_traffic_correlations` method that:
   - Accepts time range and service filter parameters
   - Queries traffic metrics across all instances using federation patterns
   - Applies time-series cross-correlation to identify service dependencies
   - Uses mutual information analysis for non-linear dependency detection
   - Implements Granger causality testing for temporal dependency inference
   - Returns correlation results with confidence scores
3. Helper methods for:
   - Time-series preprocessing and normalization
   - Cross-correlation computation with sliding windows
   - Statistical significance testing for correlation results
   - Service identity resolution across different cluster contexts
4. Proper error handling for missing data, network issues, and invalid metrics
5. Integration with existing logging and caching patterns

Follow the same architectural patterns as rca.py with clear separation of concerns.
  </action>
  <verify>
    <automated>python -c "from prometheus_mcp.dependency import TrafficCorrelator; print('TrafficCorrelator class exists')"</automated>
  </verify>
  <done>Traffic correlation analyzer identifies service dependencies through metric correlation analysis</done>
</task>

<task type="auto" tdd="true">
  <name>task 3: implement dynamic dependency graph builder</name>
  <files>src/prometheus_mcp/dependency.py</files>
  <behavior>
    - Test 1: Builds simple linear dependency chain from correlated traffic data
    - Test 2: Handles circular dependencies without infinite loops
    - Test 3: Correctly weights edges by correlation strength and confidence
    - Test 4: Resolves service identities across different cluster contexts
  </behavior>
  <action>
Implement the dynamic dependency graph builder in the dependency.py module:
1. Add `DependencyGraphBuilder` class that takes an InstanceRegistry
2. Implement `build_dependency_graph` method that:
   - Accepts correlation analysis results and service filters
   - Constructs service nodes with metadata from metric labels
   - Creates dependency edges with strength metrics from correlation analysis
   - Resolves cross-cluster service identities using instance attribution
   - Maintains graph consistency with concurrent updates
   - Returns DependencyGraph with nodes, edges, and metadata
3. Helper methods for:
   - Service identity normalization across cluster contexts
   - Graph validation and cycle detection
   - Confidence scoring for dependency relationships
   - Incremental graph updates for real-time processing
4. Proper handling of circular dependencies and complex graph structures
5. Integration with existing caching mechanisms for performance

Ensure the implementation follows the same error handling and logging patterns as other modules.
  </action>
  <verify>
    <automated>python -c "from prometheus_mcp.dependency import DependencyGraphBuilder; print('DependencyGraphBuilder class exists')"</automated>
  </verify>
  <done>Dynamic dependency graph builder creates service relationship maps from correlation data</done>
</task>

</tasks>
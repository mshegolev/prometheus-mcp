---
phase: 09-root-cause-analysis-tools
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified: [src/prometheus_mcp/rca.py, src/prometheus_mcp/models.py]
autonomous: true
requirements: [RCA-01, RCA-02, RCA-03]
user_setup: []

must_haves:
  truths:
    - AI agents can detect statistical outliers in key metrics with seasonality adjustment
    - AI agents can trace service dependencies from symptoms to potential root causes
    - AI agents can correlate recent deployments/config changes with alert onset timing
    - AI agents can rank root cause candidates based on proximity, evidence strength, and impact
    - RCA insights are integrated with existing correlation tools for enriched alert context
  artifacts:
    - path: "src/prometheus_mcp/rca.py"
      provides: "Root cause analysis engine with anomaly detection, dependency traversal, and change point detection"
      min_lines: 400
    - path: "src/prometheus_mcp/models.py"
      provides: "Data models for RCA results"
      contains: "class ChangePointDetectionResult"
    - path: "src/prometheus_mcp/models.py"
      provides: "Data models for RCA results"
      contains: "class RootCauseRankingResult"
  key_links:
    - from: "src/prometheus_mcp/rca.py"
      to: "src/prometheus_mcp/correlation.py"
      via: "correlation engine integration"
      pattern: "CorrelationEngine"
    - from: "src/prometheus_mcp/rca.py"
      to: "src/prometheus_mcp/federation.py"
      via: "cross-instance metric access"
      pattern: "fan_out_prometheus"

threat_model:
  ## Trust Boundaries

  | Boundary | Description |
  |----------|-------------|
  | Agent→MCP Server | Untrusted input from AI agents |
  | MCP Server→Prometheus Instances | Trusted outbound requests to monitored systems |

  ## STRIDE Threat Register

  | Threat ID | Category | Component | Disposition | Mitigation Plan |
  |-----------|----------|-----------|-------------|-----------------|
  | T-09-01 | Spoofing | Metric data sources | mitigate | Validate instance authenticity through registry |
  | T-09-02 | Tampering | RCA configuration | mitigate | Immutable configuration from trusted sources |
  | T-09-03 | Repudiation | Diagnostic decisions | mitigate | Detailed audit logging of all RCA operations |
  | T-09-04 | Information Disclosure | System topology | mitigate | Role-based access control on instance data |
  | T-09-05 | Denial of Service | Resource-intensive analysis | mitigate | Configurable timeouts and resource limits |
  | T-09-06 | Elevation of Privilege | Tool execution | mitigate | ReadOnlyHint enforcement in MCP annotations |

verification:
  - Change point detection correctly correlates deployment events with alert timing
  - Ranking system properly weights evidence from multiple sources
  - All RCA components handle partial instance availability gracefully
  - Integration with correlation tools maintains data consistency

success_criteria:
  - Anomaly detection engine identifies statistical outliers with configurable sensitivity
  - Dependency traversal traces service relationships from symptoms to root causes
  - Change point detection correlates deployments/config changes with alert timing
  - Ranking system orders candidates by evidence strength and impact analysis
  - Integration with correlation tools enriches alert context with RCA insights
---

<objective>
Implement change point detection and candidate ranking system for root cause analysis.

Purpose: Enable AI agents to correlate recent changes with alert timing and rank root cause candidates based on evidence strength.
Output: Complete RCA engine with change point detection and ranking capabilities
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-correlation-engine-foundation/CONTEXT.md
@.planning/phases/04-federation-fanout-merge/CONTEXT.md
@.planning/phases/02-instance-registry/CONTEXT.md

From src/prometheus_mcp/models.py:
```python
# After completion of Plan 01, these will exist:
class AnomalyDetectionResult(TypedDict):
    # ... anomaly detection fields
    
class DependencyTraversalResult(TypedDict):
    # ... dependency traversal fields
```

From src/prometheus_mcp/rca.py (after Plan 01):
```python
class AnomalyDetector:
    def __init__(self, registry: InstanceRegistry) -> None:
        # ...
        
    def detect_metric_anomalies(
        self, 
        metric_names: list[str], 
        start_time: str, 
        end_time: str,
        sensitivity: float = 0.5
    ) -> AnomalyDetectionResult:
        # ...

class DependencyTraverser:
    def __init__(self, registry: InstanceRegistry) -> None:
        # ...
        
    def traverse_from_symptoms(
        self, 
        alert_groups: dict[str, list[AMAlertItem]]
    ) -> DependencyTraversalResult:
        # ...
```

From src/prometheus_mcp/correlation.py:
```python
class CorrelationEngine:
    def __init__(self, registry: InstanceRegistry) -> None:
        self.registry = registry
        # ...
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: implement change point detection</name>
  <files>src/prometheus_mcp/rca.py</files>
  <behavior>
    - Test 1: Detects deployment events aligned with alert onset timing
    - Test 2: Correlates configuration changes with metric anomalies
    - Test 3: Handles missing deployment metadata gracefully
    - Test 4: Accounts for clock synchronization issues across instances
  </behavior>
  <action>
Implement change point detection in the rca.py module:
1. Add `ChangePointDetector` class that takes an InstanceRegistry
2. Implement `detect_change_points` method that:
   - Queries deployment and configuration change events (may require integration with external systems or metric annotations)
   - Correlates change timing with alert onset timestamps
   - Calculates correlation strength based on temporal proximity
   - Returns ChangePointDetectionResult with detailed findings
3. Helper methods for:
   - Event timeline construction from available metadata
   - Temporal alignment accounting for clock drift
   - Evidence strength calculation from multiple corroborating signals
4. Integration with existing federation patterns for cross-instance change detection
5. Fallback mechanisms for environments without change tracking

Follow the same architectural patterns as other RCA components with proper error handling.
  </action>
  <verify>
    <automated>python -c "from prometheus_mcp.rca import ChangePointDetector; print('ChangePointDetector class exists')"</automated>
  </verify>
  <done>Change point detection correlates recent deployments/config changes with alert onset timing</done>
</task>

<task type="auto">
  <name>task 2: implement candidate ranking system</name>
  <files>src/prometheus_mcp/rca.py, src/prometheus_mcp/models.py</files>
  <action>
Implement the root cause candidate ranking system:
1. Add `RootCauseRanker` class that takes an InstanceRegistry
2. Extend models.py with `RootCauseRankingResult` TypedDict including:
   - Ranked list of candidates with scores
   - Confidence intervals for rankings
   - Explanation of ranking factors
3. Implement `rank_candidates` method that:
   - Accepts inputs from anomaly detection, dependency traversal, and change point detection
   - Combines evidence from multiple sources using weighted scoring
   - Applies proximity weighting based on dependency graph distance
   - Calculates impact scores based on service criticality
   - Returns ranked list of root cause candidates
4. Helper methods for:
   - Multi-factor scoring with configurable weights
   - Confidence interval calculation for ranking reliability
   - Explanation generation for ranking decisions
5. Integration with existing correlation engine for contextual ranking

Ensure the implementation follows established patterns for extensibility and configurability.
  </action>
  <verify>
    <automated>python -c "from prometheus_mcp.rca import RootCauseRanker; hasattr(prometheus_mcp.models, 'RootCauseRankingResult') and print('RootCauseRanker class and model exist')"</automated>
  </verify>
  <done>Ranking system orders root cause candidates by evidence strength and impact analysis</done>
</task>

<task type="auto">
  <name>task 3: integrate RCA with correlation tools</name>
  <files>src/prometheus_mcp/rca.py, src/prometheus_mcp/tools_correlation.py</files>
  <action>
Enhance existing correlation tools with RCA integration:
1. Modify `correlate_alerts_across_instances` in tools_correlation.py to:
   - Optionally invoke RCA engine for deeper analysis
   - Enrich correlation results with anomaly detection findings
   - Add dependency traversal insights to cascade relationships
   - Include change point correlations in result summaries
2. Update `_format_correlation_result` to include RCA insights in markdown output:
   - Add sections for detected anomalies
   - Show dependency paths for cascades
   - Highlight correlated change events
   - Display ranked root cause candidates
3. Add helper methods in rca.py for:
   - Converting correlation results to RCA inputs
   - Formatting RCA findings for tool output
   - Handling partial RCA results when some components fail
4. Ensure backward compatibility with existing tool interfaces
5. Add proper error handling for RCA component failures

Maintain the same dual-channel output pattern (structured + markdown) as existing tools.
  </action>
  <verify>
    <automated>bash -c "cd /opt/develop/aiqa/mcps/prometheus-mcp && python -m pytest tests/test_correlation.py -v -k 'test_correlate' --tb=short"</automated>
  </verify>
  <done>RCA insights are integrated with existing correlation tools for enriched alert context</done>
</task>

</tasks>
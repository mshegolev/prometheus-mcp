---
phase: 09-root-cause-analysis-tools
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/prometheus_mcp/rca.py, src/prometheus_mcp/models.py]
autonomous: true
requirements: [RCA-01, RCA-02, RCA-03]
user_setup: []

must_haves:
  truths:
    - AI agents can detect statistical outliers in key metrics with seasonality adjustment
    - AI agents can trace service dependencies from symptoms to potential root causes
    - AI agents can correlate recent deployments/config changes with alert onset timing
    - AI agents can rank root cause candidates based on proximity, evidence strength, and impact
    - RCA insights are integrated with existing correlation tools for enriched alert context
  artifacts:
    - path: "src/prometheus_mcp/rca.py"
      provides: "Root cause analysis engine with anomaly detection, dependency traversal, and change point detection"
      min_lines: 400
    - path: "src/prometheus_mcp/models.py"
      provides: "Data models for RCA results"
      contains: "class AnomalyDetectionResult"
    - path: "src/prometheus_mcp/models.py"
      provides: "Data models for RCA results"
      contains: "class DependencyTraversalResult"
    - path: "src/prometheus_mcp/models.py"
      provides: "Data models for RCA results"
      contains: "class ChangePointDetectionResult"
  key_links:
    - from: "src/prometheus_mcp/rca.py"
      to: "src/prometheus_mcp/registry.py"
      via: "instance registry access"
      pattern: "registry\\."
    - from: "src/prometheus_mcp/rca.py"
      to: "src/prometheus_mcp/client.py"
      via: "metric querying"
      pattern: "prometheus_client\\."

threat_model:
  ## Trust Boundaries

  | Boundary | Description |
  |----------|-------------|
  | Agent→MCP Server | Untrusted input from AI agents |
  | MCP Server→Prometheus Instances | Trusted outbound requests to monitored systems |

  ## STRIDE Threat Register

  | Threat ID | Category | Component | Disposition | Mitigation Plan |
  |-----------|----------|-----------|-------------|-----------------|
  | T-09-01 | Spoofing | Metric data sources | mitigate | Validate instance authenticity through registry |
  | T-09-02 | Tampering | RCA configuration | mitigate | Immutable configuration from trusted sources |
  | T-09-03 | Repudiation | Diagnostic decisions | mitigate | Detailed audit logging of all RCA operations |
  | T-09-04 | Information Disclosure | System topology | mitigate | Role-based access control on instance data |
  | T-09-05 | Denial of Service | Resource-intensive analysis | mitigate | Configurable timeouts and resource limits |
  | T-09-06 | Elevation of Privilege | Tool execution | mitigate | ReadOnlyHint enforcement in MCP annotations |

verification:
  - All new classes and functions have comprehensive docstrings
  - RCA engine integrates with existing InstanceRegistry pattern
  - Anomaly detection handles missing/invalid metric data gracefully
  - Dependency traversal correctly handles circular dependencies
  - Change point detection accounts for clock synchronization issues

success_criteria:
  - Anomaly detection engine identifies statistical outliers with configurable sensitivity
  - Dependency traversal traces service relationships from symptoms to root causes
  - Change point detection correlates deployments/config changes with alert timing
  - Ranking system orders candidates by evidence strength and impact analysis
  - Integration with correlation tools enriches alert context with RCA insights
---

<objective>
Implement root cause analysis tools for anomaly detection, dependency traversal, and change point identification.

Purpose: Enable AI agents to autonomously diagnose underlying causes of alert cascades by analyzing metric anomalies, tracing service dependencies, and correlating with recent changes.
Output: New RCA engine module with integration into existing correlation tools
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-correlation-engine-foundation/CONTEXT.md
@.planning/phases/04-federation-fanout-merge/CONTEXT.md
@.planning/phases/02-instance-registry/CONTEXT.md

From src/prometheus_mcp/models.py:
```python
class QueryRangeOutput(TypedDict):
    query: str
    start: str
    end: str
    step: str
    result_type: str
    series_count: int
    total_points: int
    truncated: bool
    data: list[RangeSeries]

class RangeSeries(TypedDict):
    labels: dict[str, str]
    point_count: int
    values: list[list[float | str]]
```

From src/prometheus_mcp/correlation.py:
```python
class CorrelationEngine:
    def __init__(self, registry: InstanceRegistry) -> None:
        self.registry = registry
        self.matcher = AlertMatcher()
        self.grouper = AlertGrouper()
        self.cascade_detector = CascadeDetector()
```
</context>

<tasks>

<task type="auto">
  <name>task 1: extend models with RCA result types</name>
  <files>src/prometheus_mcp/models.py</files>
  <action>
Extend the models.py file with new TypedDict classes for RCA results:
1. Create `AnomalyDetectionResult` with fields for anomalies found, metric information, anomaly scores, and timestamps
2. Create `DependencyTraversalResult` with fields for traversal paths, evidence weights, and impact analysis
3. Create `ChangePointDetectionResult` with fields for change events, correlation strength, and timing analysis
4. Create `RootCauseCandidate` for ranking system with fields for candidate identification, evidence score, and impact assessment
5. Ensure all new models follow the existing pattern with proper type hints and documentation

Structure these models to be compatible with the existing correlation result formats and follow the same design patterns.
  </action>
  <verify>
    <automated>python -c "import prometheus_mcp.models; hasattr(prometheus_mcp.models, 'AnomalyDetectionResult') and hasattr(prometheus_mcp.models, 'DependencyTraversalResult') and hasattr(prometheus_mcp.models, 'ChangePointDetectionResult')"</automated>
  </verify>
  <done>All RCA result models are defined with proper type hints and documentation</done>
</task>

<task type="auto">
  <name>task 2: implement anomaly detection engine</name>
  <files>src/prometheus_mcp/rca.py</files>
  <action>
Create a new rca.py module implementing the anomaly detection engine with these components:
1. `AnomalyDetector` class that takes an InstanceRegistry in its constructor
2. `detect_metric_anomalies` method that:
   - Accepts metric names, time range, and sensitivity parameters
   - Queries metrics across all instances using federation patterns
   - Applies statistical process control with seasonal decomposition
   - Identifies outliers using z-score and interquartile range methods
   - Returns AnomalyDetectionResult with detailed findings
3. Helper methods for:
   - Seasonal decomposition using moving averages
   - Outlier detection with configurable thresholds
   - False positive reduction through ensemble methods
4. Proper error handling for missing data, network issues, and invalid metrics
5. Integration with existing logging and caching patterns

Follow the same architectural patterns as correlation.py with clear separation of concerns.
  </action>
  <verify>
    <automated>python -c "from prometheus_mcp.rca import AnomalyDetector; print('AnomalyDetector class exists')"</automated>
  </verify>
  <done>Anomaly detection engine implements statistical outlier detection with seasonality adjustment</done>
</task>

<task type="auto" tdd="true">
  <name>task 3: implement dependency traversal algorithm</name>
  <files>src/prometheus_mcp/rca.py</files>
  <behavior>
    - Test 1: Traverses simple linear dependency chain and returns correct path
    - Test 2: Handles circular dependencies without infinite loops
    - Test 3: Weights paths by correlation strength and returns ranked results
    - Test 4: Correctly identifies root causes with high evidence scores
  </behavior>
  <action>
Implement the dependency traversal algorithm in the rca.py module:
1. Add `DependencyTraverser` class that takes an InstanceRegistry
2. Implement `traverse_from_symptoms` method that:
   - Accepts alert groups from correlation engine
   - Builds service dependency graph from metric correlations
   - Performs breadth-first search from affected services upstream
   - Calculates evidence weights based on correlation strength
   - Returns DependencyTraversalResult with ranked paths
3. Helper methods for:
   - Dependency graph construction from metric correlations
   - Path scoring based on evidence strength and temporal proximity
   - Impact analysis for prioritizing investigation paths
4. Proper handling of circular dependencies and complex graph structures

Ensure the implementation follows the same error handling and logging patterns as other modules.
  </action>
  <verify>
    <automated>python -c "from prometheus_mcp.rca import DependencyTraverser; print('DependencyTraverser class exists')"</automated>
  </verify>
  <done>Dependency traversal algorithm traces service dependencies from symptoms to potential root causes</done>
</task>

</tasks>